# Crystal Quiz Challenge — File Manifest
Manifest version: 1.29
Last updated: 2026-05-25

| File | Version | Last Updated | Purpose | Status |
|------|---------|--------------|---------|--------|
| index.html | 1.8.2 | 2026-05-25 | All game screens. v1.8.2: peso removed from player UI (player dashboard `pdc-peso` + Crystal Wallet `wallet-peso` spans deleted per SPEC v3.3 Part 12I — host UI keeps peso); pre-game pokeball copy "3 Pokeball(s)" → "1 Pokeball" per SPEC Part 11 P8. v1.8.1: mascot alt text + Region 10 Game Complete screen. v1.8: player dashboard three-column rewrite. v1.7.2: Room Detail Overlay Start Game + drop Copy Code. v1.7.1: lobby cap label 0/8 → 0/5. v1.7: host dashboard three-column. v1.6: persistent identity. v1.5: wallet + host crystal panels. v1.4: removed "Continue Journey". v1.3: host-landing. v1.2: gym-review. v1.1: test-build lid. | Active |
| style.css | 1.7.2 | 2026-05-25 | Styling. v1.7.2: team-cap UI for v3.3 Part 12E — `.pdc-team-header`, `.pdc-team-cap-hint`, `.pdc-pokemon-row.empty` (dashed empty slot), `.pdc-pokemon-row.locked` (greyed "🔒 Reach Level N"), `.pdc-poke-release` (red trade-in pill), `.pdc-poke-starter-tag`. v1.7.1: dead `.region-card.coming-soon` block removed. v1.7: player three-column dashboard — `.pdc-3col-screen`, `.pdc-header` (left identity / right balance+logout), `.pdc-3col` grid, `.pdc-col` sticky header + scrollable body, `.pdc-section`, `.pdc-room-card` (active/pending/archived variants + `.highlight`), `.pdc-balance-card`, `.pdc-ledger-tabs` + `.pdc-tab.active`, `.pdc-ledger-row`, `.pdc-top-scores` + `.pdc-score-card`, `.pdc-pokemon-group` + rarity pills, `.pdc-broadcast-text` + meta, `.pdc-abandon-inline` / `.pdc-abandon-pending`, `.pdc-redeem-form` / `.pdc-redeem-pending`. v1.6.1: `.wl-slots-grid` repeat(5,1fr). v1.6: three-column host dashboard. v1.5: persistent-identity surfaces. v1.4: wallet + host crystal panels. v1.3: host-landing + presence. v1.2: gym-review. v1.1: `.region-card.coming-soon`. | Active |
| game.js | 1.17 | 2026-05-25 | Game logic. v1.17: **economy engine v3.3 LIVE (SPEC Part 12)** — Path B per-gym earning (replaces per-question award; settles once in endGym using `baseCrystals × correct/10 + speedBonus`, then 8/9/10 badge multiplier ×1.0/1.5/2.0; 6.7× UAT reduction confirmed via sim). New constants: `BALL_COST` (50/150/400/1000/2500), `CATCH_RATE`, `REDEEM_BASE` (20/80/200/500/1200), `TEAM_CAP_BY_LEVEL { 1:3, 2:3, 3:4, 4:4, 5:5 }`, `MAX_TEAM_CAP=5`, `PITY_MISS_THRESHOLD=3`, `RELEASE_MIN/MAX_PCT`. New helpers: `playerLevelFromBadges`, `currentPlayerLevel`, `currentTeamCap`, `ballCostForRarity`, `redeemBaseForRarity`, `redeemValueFor`, `tradeInValueFor`, `xpRatioOf`, `getPityCounter`/`bumpPityCounter`/`resetPityCounter`. `buyRegionalPokeball` cost now reads target rarity (was region.pokeball); team-cap soft-block. `pdcRenderPokemonTeam` rewritten to render all 5 slots — filled/empty/locked variants with `🔒 Reach Level N` future-slot UI + per-Pokemon `🔄 Release · trade-in` button (starters cannot release). New `pdcConfirmRelease` writes positive `adjustment` ledger row scaled 40→80% by xpRatio; warns "gone forever". `attemptRegionalCatch` consults the pity counter and draws from R-1 catch_bank when ≥3 consecutive misses on the same pokemon. New Pokemon born at `currentPlayerLevel()` with `{xp:0, xpCap:100, xpRatio:0}` fields. Pre-game pokeballs 3 → 1 (Part 11 P8). MULTIPLY/DOUBLE_OR_NOTHING/STEAL ability bodies stripped to no-op stubs (data is gone in v2.0; safety net). REGIONS table: removed `pokeball` field (rarity-based now) and `badgeMin` field (8/10 accuracy badge per Part 1) and recalibrated `speedMax` to Part 2 numbers (20%-of-base cap). Dead `TIER_BASE`/`TIER_SPEED_MAX` constants removed. checkAnswer per-question mutation gone — display-only "+💎 (settles at gym end)" feel-good text. v1.16.3: defensive `verifyQuestionBanks(qData, band)` walks Regions 1..10 on loadQuestions, logs `[BANK CHECK]` for every region and `[MISSING BANK] Region N has no questions for <band>` if any region is short on blueprints / slots / catch_bank. Populates `STATE._missingBankRegions`; `showMap` is now async + preloads questions and flags any missing region with `⚠️ No questions yet` + non-clickable card instead of crashing. Audit ran: all 10 regions in both junior + senior banks are full (5 gyms × 10 slots, 15+ catch each). v1.16.2: ledger insert error path hardened — `dbLedgerInsert` now wraps the Supabase call in `try/catch`, logs `[LEDGER WRITE FAILED]` on all three failure paths (Supabase error / no row returned / thrown exception), and returns null. Redeem submit paths (`pdcSubmitRedeem`, `walletSubmitRedeem`) now check the returned row — if null, log `[REDEEM WRITE] INSERT FAILED` and show the player a real error instead of the false "Request sent" confirmation. Host pending fetch (`dbLedgerPending`) split-logs `[CRYSTAL REQUESTS] { total, redemptions, abandons, all }` so the user can spot exactly which rows landed. v1.16.1: ledger pipeline diagnostics + multi-pending host bug-fix. `dbLedgerForPlayer` logs `[LEDGER CHECK] <pid> entries: <n>` on every fetch; `dbLedgerPending` logs `[REDEEM READ] pending requests: <n>`; `pdcSubmitRedeem` + `walletSubmitRedeem` log `[REDEEM WRITE] <row>`; `renderPlayerDashboard` logs `[SAVE CHECK] <pid> crystals: <n>` and triggers `balanceFromLedger()` on every mount. Host bug-fix: `pendingByPid` now stores an ARRAY of pending rows per player (was single entry) — `col2AccountCard` renders one block per pending row, so a player with both a redemption AND an abandon request shows both. v1.16: ledger sync + pause freeze + TIME cap + Phase 2 unlock. New `balanceFromLedger(playerId)` invariant helper (sums approved+modified ledger rows, compares to `total_crystals`, logs `[LEDGER DRIFT]` on divergence) — called after every mutation (STEAL pair, MULTIPLY/DOUBLE_OR_NOTHING deltas, Pokeball purchase, broadcast send, endGym). `checkAnswer` now writes per-ability `adjustment` rows for the DELTA contributed by MULTIPLY (`note:'MULTIPLY ability used'`) and DOUBLE_OR_NOTHING (`note:'DOUBLE_OR_NOTHING — won/lost'`); base earn row from `endGym` is unchanged. Pause path: `checkPauseState` now flips `STATE.paused`, snapshots `STATE.pausedTimeRemaining`, clears the timer interval, freezes the displayed countdown; resume restarts from the snapshot. `timeUp` guard `if (STATE.paused) return;` at the top swallows late ticks. TIME ability speed-bonus cap: `STATE.originalTimeLimit` set once in `loadQuestion`, never touched by `applyAbilityTime`; `checkAnswer` clamps `effectiveTimeLeft` to it. Phase 2 unlock: `MAX_PLAYABLE_REGION = 10`, region-card "coming soon" pill removed, `finishRegionalCatch` flips room to `phase:'GAME_OVER'` + `archived:true` on Region 10 clear and routes to repurposed final screen via `showFinalCompleteScreen` (alias `showTestBuildComplete` retained). v1.15: player three-column dashboard + abandon + broadcast + journey aggregation. v1.14.1: banner hides when no active room. v1.14: rdStartGame + drop rdCopyCode. v1.13: host entry routing + `MAX_PLAYERS = 5`. v1.12: three-column host. v1.11: persistent identity. v1.10: crystal-banking. v1.9: solo paths removed. v1.8: rejoin + heartbeat + host manager. v1.7: Review Mode. v1.6.1: seen-question-set + unscramble. | Active |
| questions.json | 3.0 | 2026-05-22 | Archived source library (590, blueprint+bank format) — not loaded at runtime | Archived |
| questions-junior.json | 3.3 | 2026-05-23 | Junior bank (9-11), 729 questions: 590 source + 49 R1-3 gym topups + 90 catch topups; every region catch >=15, pokeball=30, every R1-3 bucket >=6. v3.3: in-file version string synced to manifest (all 10 regions fully populated). | Active |
| questions-senior.json | 3.3 | 2026-05-23 | Senior bank (12-13), 729 questions: 590 source + 49 R1-3 gym topups + 90 catch topups; every region catch >=15, pokeball=30, every R1-3 bucket >=6. v3.3: in-file version string synced to manifest. | Active |
| pokemon.json | 2.1 | 2026-05-25 | Pokemon library — **v2.0 data + Part 12D economy fields populated** (every regional entry now carries `baseValue` per rarity: Basic 20 / Holo 80 / Rare 200 / Super 500 / Legendary 1200; starters carry `baseValue: 0`). Engine v3.3 now consumes these via `redeemValueFor` / `tradeInValueFor`. Otherwise identical to v2.0 baseline: 10 starters + 183 evolution-line regional + 18-entry `bench`. Tier rename common→basic + new `holo` tier. `abilityEffect` removed; replaced by `move{type,phase,description}`. 6 MOVE types only (CLUE/CLOCK/SWAP/EXTRA_SHOT/TIME_TRAVEL/ELIMINATE) with strict rarity-pool legality. New per-entry fields: `lineName`, `catchForm`, `evolutionChain[]`, `stages` (1-3), `battleAbility:null` (deferred), `redeemValue:null` (deferred; starters=0). R10 = 🇵🇭 Pilipinas (10 Filipino-mythology legendaries + 8 PH endemic fauna, all carry `loreNote`). Tier totals 45/30/37/42/29 = 183. Legendary spread R7=3, R8=7, R9=9, R10=10. 18/18 validation checks pass. v1.1: Groudon corrected to legendary; 5 ability names renamed for uniqueness. | Active |
| gengar.png | 1.1 | 2026-05-22 | Home mascot — new mascot image (Pikachu vs Gengar battle scene), 1024×1024 PNG | Active |
| MIGRATIONS.md | 1.2 | 2026-05-23 | Supabase SQL migrations. v1.2 adds the `crystal_ledger` RLS policy block — three policies (anon insert + select + update) for the pre-launch "anon can do anything" posture. Use this when `[LEDGER WRITE FAILED] permission denied for table crystal_ledger` shows up in the browser console. v1.1: persistent-identity block (clean-slate DELETEs + new columns + CHECK constraint on `player_id`). v1.0: `crystal_ledger` create-table + indexes. Run blocks in order in Supabase Studio. | Active |
| CLAUDE.md | 0.6.0 | 2026-05-25 | Master project doc — BUILD VERSION 0.5.0 → 0.6.0 (v3.3 economy LIVE). Header `SYNCED TO SPEC: v3.3` now reads "economy v3.3 LIVE — Part 12 implemented". Status table "Economy" row flipped ⬜ designed v3.3 → ✅ LIVE. Body still intentionally describes the pre-redesign build sections; rewriting those is a later doc pass per Document Hygiene. | Active |
| FILES.md | 1.29 | 2026-05-25 | This manifest | Active |
| CRYSTAL_QUIZ_REDESIGN_SPEC.md | v3.3 | 2026-05-25 | Design bible. v3.3 adds Part 12 (economy): Path B per-gym earning, rarity-based pokeball ladder, redeem value (`baseValue × (1 + xpRatio × 0.5)`), trade-in 40→80%, pity softener, cap GROWS 3→4→5 by player level (supersedes v3.2 cap=3), lifetime-wallet + redeem-and-burn persistence (12I), peso visibility split. Earlier (v3.2) locks: Part 11 catch principles, Part 10 rarity, R10 Philippine theme, cosmetic evolution. | Active |
| POKEMON_LIBRARY_v2.md | 1.0 | 2026-05-24 | Roster reference for Library v2.0: per-region tables (183 lines), bench (18 displaced), R10 cultural notes. Companion to REDESIGN_SPEC Part 11. | Active |
| CLAUDE_CODE_PROMPT_v2_FINAL.md | 1.0 | 2026-05-24 | The apply prompt for Library v2.0 — kept in repo for trace / future re-runs. | Active |
| CLAUDE_CODE_PROMPT_economy_engine.md | 1.0 | 2026-05-25 | The apply prompt for SPEC v3.3 Part 12 economy engine — kept in repo for trace. | Active |
| CLAUDE_CODE_PROMPT_peso_visibility.md | 1.0 | 2026-05-25 | The apply prompt for SPEC v3.3 Part 12I peso-visibility split — kept in repo for trace. | Active |

Maintain this manifest on every future file change — it's the single
source of truth for what's deployed.
