# Crystal Quiz Challenge — File Manifest
Manifest version: 1.20
Last updated: 2026-05-22

| File | Version | Last Updated | Purpose | Status |
|------|---------|--------------|---------|--------|
| index.html | 1.7.1 | 2026-05-22 | All game screens. v1.7.1: lobby cap label "0 / 8 players joined" → "0 / 5 players joined". v1.7: host dashboard rewritten as three-column landscape layout — `screen-host` body replaced with `host-persistent-banner` + `.host-3col` grid (ROOMS / CRYSTALS / CONTROLS); added `room-detail-overlay`, `ledger-modal-overlay`, `confirm-dialog-overlay`, `player-broadcast-banner`. v1.6: persistent identity. v1.5: wallet + host crystal panels. v1.4: removed "Continue Journey". v1.3: host-landing. v1.2: gym-review. v1.1 (1.6): test-build lid. | Active |
| style.css | 1.6.1 | 2026-05-22 | Styling. v1.6.1: `.wl-slots-grid` from `repeat(4, 1fr)` → `repeat(5, 1fr)` (5-player single row). v1.6: three-column host dashboard — `.host-3col-screen`, `.host-3col` grid, `.host-col` + sticky header + scrollable body, `.col1-*` / `.col2-*` / `.col3-*` blocks, `.room-card-mini`, `.account-card` + `.ac-*`, `.modal-overlay` + `.modal-card` (scoped via `.modal-overlay > .modal-card`), `.room-detail-card`, `.ledger-modal-card`, `.confirm-dialog-card`, `.rd-player`, `.lm-row`, `.player-broadcast-banner`. v1.5: persistent-identity surfaces. v1.4: wallet + host crystal panels. v1.3: host-landing + presence. v1.2: gym-review. v1.1 (1.6): `.region-card.coming-soon`. | Active |
| game.js | 1.13 | 2026-05-22 | Game logic. v1.13: host entry routing — `?host=true` bypasses `screen-host-landing` and lands directly on the three-column dashboard via new `showHostDashboardUnscoped`; `checkHostMode` no-room branch resets HOST.* scoped fields then calls it. `renderHostDashboard` is now async + awaits col 1 so persistent banner can fall back to `HOST_UI.activeRoomCode` on first tick (hides if no room exists). New `MAX_PLAYERS = 5` constant — replaces hard-coded `8` in waiting-lobby slot loop + "Room is full" guard. v1.12: three-column host dashboard logic — `HOST_UI` state, `renderCol1Rooms` / `renderCol2Accounts` / `renderCol3Controls`, room-detail overlay, ledger modal, generic `confirmDialog`, column handlers (`col1*`/`col2*`/`col3*`), `dbLoadAllPlayersFull`; `playerJoin` rejects when `room.locked`; `checkPauseState` reads `room.announcement` + `renderPlayerBroadcast` banner. v1.11: persistent player identity. v1.10: crystal-banking. v1.9: solo paths removed. v1.8: rejoin + heartbeat + host manager. v1.7: Review Mode. v1.6.1: seen-question-set + unscramble. | Active |
| questions.json | 3.0 | 2026-05-22 | Archived source library (590, blueprint+bank format) — not loaded at runtime | Archived |
| questions-junior.json | 3.3 | 2026-05-22 | Junior bank (9-11), 729 questions: 590 source + 49 R1-3 gym topups + 90 catch topups; every region catch >=15, pokeball=30, every R1-3 bucket >=6 | Active |
| questions-senior.json | 3.3 | 2026-05-22 | Senior bank (12-13), 729 questions: 590 source + 49 R1-3 gym topups + 90 catch topups; every region catch >=15, pokeball=30, every R1-3 bucket >=6 | Active |
| pokemon.json | 1.1 | 2026-05-22 | Pokemon library: 10 starters + 100 regional. Every Pokemon has a unique ability NAME and unique effect across 10 mechanics; rarity scales power; baseValue inflates per region. v1.1: Groudon corrected to legendary; 5 ability names renamed for uniqueness | Active |
| gengar.png | 1.1 | 2026-05-22 | Home mascot — new mascot image (Pikachu vs Gengar battle scene), 1024×1024 PNG | Active |
| MIGRATIONS.md | 1.1 | 2026-05-22 | Supabase SQL migrations. v1.1 adds the persistent-identity block (clean-slate DELETEs + new columns + CHECK constraint on `player_id`). v1.0: `crystal_ledger` create-table + indexes. Run blocks in order in Supabase Studio. | Active |
| CLAUDE.md | 0.4.2 | 2026-05-22 | Master project doc — adds host entry-flow note: `?host=true` bypasses `screen-host-landing` and lands directly on the three-column dashboard (Column 1 = all rooms, Column 2 = all accounts, Column 3 = most-recent active room). Includes evening 2026-05-22 session summaries + "Host Dashboard — three-column landscape layout" section. Phase 3 Backlog replaced with Next-Session-High-Priority list + Phase 4 Checklist. Covers Persistent Player Identity + Crystal Banking + multiplayer-only + post-Phase-1 UAT additions. | Active |
| FILES.md | 1.20 | 2026-05-22 | This manifest | Active |

Maintain this manifest on every future file change — it's the single
source of truth for what's deployed.
