# Crystal Quiz Challenge — File Manifest
Manifest version: 1.23
Last updated: 2026-05-23

| File | Version | Last Updated | Purpose | Status |
|------|---------|--------------|---------|--------|
| index.html | 1.8 | 2026-05-23 | All game screens. v1.8: player dashboard rewritten as three-column landscape layout — `screen-home` slimmed to two buttons (🆕 Create Account / 🔑 Log In, no Join/Rules/Board/Settings); `screen-player-dashboard` replaced with `pdc-header` + `.pdc-3col` grid (ROOMS / CRYSTAL WALLET / MY JOURNEY); added `pdc-room-detail-overlay` (read-only). v1.7.2: Room Detail Overlay actions — added Start Game (lobby-only), removed Copy Code. v1.7.1: lobby cap label 0/8 → 0/5. v1.7: host dashboard three-column. v1.6: persistent identity. v1.5: wallet + host crystal panels. v1.4: removed "Continue Journey". v1.3: host-landing. v1.2: gym-review. v1.1: test-build lid. | Active |
| style.css | 1.7 | 2026-05-23 | Styling. v1.7: player three-column dashboard — `.pdc-3col-screen`, `.pdc-header` (left identity / right balance+logout), `.pdc-3col` grid, `.pdc-col` sticky header + scrollable body, `.pdc-section`, `.pdc-room-card` (active/pending/archived variants + `.highlight`), `.pdc-balance-card`, `.pdc-ledger-tabs` + `.pdc-tab.active`, `.pdc-ledger-row`, `.pdc-top-scores` + `.pdc-score-card`, `.pdc-pokemon-group` + rarity pills, `.pdc-broadcast-text` + meta, `.pdc-abandon-inline` / `.pdc-abandon-pending`, `.pdc-redeem-form` / `.pdc-redeem-pending`. v1.6.1: `.wl-slots-grid` repeat(5,1fr). v1.6: three-column host dashboard. v1.5: persistent-identity surfaces. v1.4: wallet + host crystal panels. v1.3: host-landing + presence. v1.2: gym-review. v1.1: `.region-card.coming-soon`. | Active |
| game.js | 1.15 | 2026-05-23 | Game logic. v1.15: player three-column dashboard — new `PLAYER_UI` state, rewritten `openPlayerDashboard` / `renderPlayerDashboard` as a dispatcher calling `pdcRenderHeader` + `pdcRenderCol1Rooms` + `pdcRenderCol2Wallet` + `pdcRenderCol3Journey`. Col 1: `pdcRoomCard` with active/pending/archived variants, abandon flow (`pdcRequestAbandon`/`pdcConfirmAbandon` writes `type:'adjustment',amount:0,note:'Abandon request — …'`), read-only Room Detail Overlay. Col 2: balance card, redeem form (pending banner), filtered ledger (`pdcSetLedgerFilter` All/Earned/Redeemed, Show more pagination). Col 3: aggregated top gym scores (sorted by crystals desc, medals 🥇🥈🥉), all-rarities Pokemon grouping, broadcast box (10💎 cost, balance-after preview, deducts via `dbBumpCrystals(-10)`, audit ledger row, writes `room.announcement`). Host: new `col2ApproveAbandon` writes `save.abandoned_rooms` and `col2PendingBlock` branches by request type. Entry routing: `window.load` skips `screen-home` when `cqc_player_id` valid (auto `dbLoginPlayer` + `openPlayerDashboard`). Pokemon catches + gym results now tag `roomCode`. Every gym attempt writes a ledger row (`amount=0` for fails). 15s `pdcStartPoll` refresh. v1.14.1: banner hides when no active room. v1.14: rdStartGame + drop rdCopyCode. v1.13: host entry routing + `MAX_PLAYERS = 5`. v1.12: three-column host. v1.11: persistent identity. v1.10: crystal-banking. v1.9: solo paths removed. v1.8: rejoin + heartbeat + host manager. v1.7: Review Mode. v1.6.1: seen-question-set + unscramble. | Active |
| questions.json | 3.0 | 2026-05-22 | Archived source library (590, blueprint+bank format) — not loaded at runtime | Archived |
| questions-junior.json | 3.3 | 2026-05-22 | Junior bank (9-11), 729 questions: 590 source + 49 R1-3 gym topups + 90 catch topups; every region catch >=15, pokeball=30, every R1-3 bucket >=6 | Active |
| questions-senior.json | 3.3 | 2026-05-22 | Senior bank (12-13), 729 questions: 590 source + 49 R1-3 gym topups + 90 catch topups; every region catch >=15, pokeball=30, every R1-3 bucket >=6 | Active |
| pokemon.json | 1.1 | 2026-05-22 | Pokemon library: 10 starters + 100 regional. Every Pokemon has a unique ability NAME and unique effect across 10 mechanics; rarity scales power; baseValue inflates per region. v1.1: Groudon corrected to legendary; 5 ability names renamed for uniqueness | Active |
| gengar.png | 1.1 | 2026-05-22 | Home mascot — new mascot image (Pikachu vs Gengar battle scene), 1024×1024 PNG | Active |
| MIGRATIONS.md | 1.1 | 2026-05-22 | Supabase SQL migrations. v1.1 adds the persistent-identity block (clean-slate DELETEs + new columns + CHECK constraint on `player_id`). v1.0: `crystal_ledger` create-table + indexes. Run blocks in order in Supabase Studio. | Active |
| CLAUDE.md | 0.5.0 | 2026-05-23 | Master project doc — adds "Player Dashboard — three-column landscape layout" section (Game Rooms / Crystal Wallet / My Journey, abandon flow, broadcast pipeline). screen-home is now login-only; localStorage skips it. Bumps to 0.5.0 — Phase 1 + Phase 2 player surface complete; Phase 3 begins next. | Active |
| FILES.md | 1.23 | 2026-05-23 | This manifest | Active |

Maintain this manifest on every future file change — it's the single
source of truth for what's deployed.
